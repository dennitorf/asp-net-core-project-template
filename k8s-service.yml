---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: ms-name
  name: ms-name
  namespace: default
spec:
  ports:
  - name: ms-name
    port: 80
    targetPort: service-port
    protocol: TCP
  selector:
    app: ms-name